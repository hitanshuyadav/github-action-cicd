#name of the workflow
name: code

#when the workflow should run
on:
  push:

jobs:
  code-checkout:
    runs-on: ubuntu-latest
    steps:
      - name: clone the code
        uses: actions/checkout@v7

      - name: print message
        run: echo "code is cloned"
    
